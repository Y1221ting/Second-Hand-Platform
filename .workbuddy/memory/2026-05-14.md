# 2026-05-14 工作日志

## 安全漏洞修复
- **DELETE /api/users/:userId 无认证** → 添加 authMiddleware + 本人userId校验
- **PUT /api/users/:userId 越权** → 添加 `req.user._id.toString() !== userId` 检查
- **JWT密钥两处硬编码** → 统一从 `config/auth.js` 的 `SECRET` 引入
- **UserProfile.js displayEdit** → 从 `user && user.id` 改为 `user && user.id === id`
- **购买弹窗数据丢失** → ProductDetails.js handleConfirmPurchase 添加 `body: JSON.stringify(userData)`
- **Product模型索引不生效** → 调整顺序：index() 定义在 model() 之前，新增 `{createdAt: -1}` 索引
- **updateProductById 缺 runValidators** → 补上

## 数据库崩溃修复（本轮新增）
### 1. 购买原子化（防竞态超卖）
- `purchaseProduct` 从 findById → 检查 → save() 改为 `findOneAndUpdate` + `$inc: { quantity: -1 }`
- MongoDB 单文档原子操作，并发下不会超卖

### 2. base64 图片迁移到文件存储（防 16MB 爆库）
- 新增 `Server/controllers/uploadController.js` — multer 磁盘存储，5MB/张限制
- 新增 `Server/routes/uploadRoutes.js` — POST /api/upload
- `Server/server.js` — 注册上传路由 + `/uploads` 静态文件服务 + 自动创建 uploads 目录
- `Client/src/components/AddProduct.js` — 改为先上传图片到 `/api/upload` 获取 URL，再提交商品
- `Client/nginx.conf` — 增加 `/uploads/` 代理到后端
- `Server/.gitignore` — 忽略上传文件
- 创建 `Server/uploads/` 目录 + .gitkeep

## 更新文档
- PROJECT_SUMMARY.md 更新：新增购买原子化+图片迁移修复记录，更新图片存储说明
