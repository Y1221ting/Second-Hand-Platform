# 2026-05-24

## 项目优化修复（多轮迭代）

完成一批安全与体验优化：
1. **loginUser 返回密码哈希** — toObject() 后 delete password
2. **getAllUsers + getUserById 密码保护** — .select("-password") 及返回前删除
3. **ProductCard `<a>` → `<Link>`** — 避免整页刷新
4. **docker-compose 密钥硬编码移入 .env** — ${变量} 引用
5. **移动端搜索** — Navbar 右侧+搜索图标，点击展开输入框
6. **body-parser 50MB → 10MB** — 降低 DoS 风险
7. **deleteUser 级联孤儿商品** — Product.updateMany 设为 inactive
8. **价格输入 alert → 内联红色提示** — AddProduct.js 状态驱动

10. **auth.js createSession 返回值扁平化** — 返回 token 字符串，响应变 { token: "xxx" }
11. **CHANGELOG.md** — 追加 1.17.0 版本记录
12. **PROJECT_SUMMARY.md / STUDY.md** — 同步更新，修复过时信息
13. **修复 Login.js data.token.token bug** — 后端返回扁平结构后前端还是错误的 data.token.token，改为 data.token
14. **STUDY.md Q&A 大扩充** — 从 10 个问题增至 45 个，按架构/后端/前端/数据库/AI/安全/部署/业务逻辑/未来规划分类
15. **STUDY.md 修正 bcrypt 轮数** — 12 → 8（实际代码用 bcryptjs 8 轮）
16. **新增 AI面试官提示词.md** — 发给其他 AI 用来模拟答辩面试的提示词
