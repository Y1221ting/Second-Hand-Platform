## 22日 — 10项修复（导航、搜索、布局、购物车等）
- 导航文案修正：Footer「首页」→「网站首页」、Navbar「我的交易」→「个人中心」
- 搜索优化：顶部搜索框读取URL参数生效，Filters左侧搜索框移除
- ProductList Grid 响应式布局代替 flex-wrap
- Profile/ProductList 紧凑横条布局
- authContext 同步初始化 localStorage 解决刷新退出
- UserProfile 挂载时获取购物车数据
- User Schema 加 timestamps
- UserDetails 删除 city/state/zipCode，统一走 address
- cartController removeFromCart/updateQuantity 返回 populate 后的购物车

## 22日补 — 商品卡片分两行布局 + 学院模糊搜索
- ProductCard 按钮和价格从同一行改为分两行（方案A），价格居中独立显示
- 价格显示从 toFixed(2) 改为 toFixed(1)（角为单位），前端 capped at 9999.9
- 后端 college 筛选从精确匹配改为 $regex 模糊搜索
- 左侧学校下拉框选学校现在会触发搜索（不再是摆设）
- 涉及文件：ProductCard.js, ProductDetails.js, productController.js, Filters.js, Home/index.js

## 22日补2 — 搜索范围优化 + 价格上限校验
- 关键字搜索从 name+description 改为 name + uploadedBy.college + uploadedBy.name，不再搜 description
- 价格上限 9999.9：后端 createProduct/updateProductById 校验拒绝、前端 AddProduct/EditProduct 输入弹窗 + input max/step
- 涉及文件：productController.js, AddProduct.js, EditProduct.js, ProductForm.js

## 22日补3 — 推荐系统（阶段一：规则引擎）
- 后端 GET /api/products/recommendations：同类目 → 同校 → 同卖家 → 最新兜底，接受 excludeId/category/college/sellerId 参数
- 推荐模块从首页商品列表移到商品详情页底部，改为"猜你喜欢"
- Recommendations 组件完全静默：无数据不渲染，失败不报错
- 涉及文件：productController.js, Recommendations.js, ProductDetails.js, Home/index.js
- 未登录用户显示最新商品，登录用户优先推荐同校同学发布的商品
- URL 路由须注意 /recommendations 放在 /:id 之前
- 涉及文件：productController.js, productRoutes.js, Home/Recommendations.js, Home/index.js
