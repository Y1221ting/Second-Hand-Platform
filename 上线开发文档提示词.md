请扮演一个资深互联网项目技术负责人，基于以下项目信息，为我生成一份面向生产上线（Production-Ready）的完整开发文档。

## 项目概述

**项目名称**：Second-Hand 校园二手交易平台
**当前状态**：核心功能已开发完成，正在进行重构简化（v3 阶段），尚未正式上线运营
**技术栈**：React 18 + TailwindCSS（前端）、Node.js + Express（后端）、MongoDB 7.0（数据库）、Docker Compose 部署
**部署环境**：阿里云 Ubuntu 22.04，2 核 2G，Nginx 反代 + Docker 容器
**上线域名**：freevian.top

## 服务器配置现状

- 服务器：8.162.24.145（Ubuntu 22.04，2 核 2G）
- 系统 Nginx 监听 80 端口，反向代理规则：
  - `/api/` 和 `/uploads/` → 后端容器 8000 端口
  - 其余请求 → 前端容器 5000 端口
- Docker Compose 三容器：frontend（Nginx + React SPA）、backend（Express）、mongodb（MongoDB 7.0）
- MongoDB 容器仅监听 127.0.0.1:27017，不对外暴露
- 另有备用服务器 47.96.17.173（2 核 2G），暂未使用

## 技术栈详情

- 前端：React 18 + TailwindCSS，Create React App 构建，Nginx 容器吐静态文件
- 后端：Express 4.18，Mongoose 7.5，JWT 认证，Multer 文件上传
- 数据库：MongoDB 7.0，含用户、商品、购物车三个集合
- 安全中间件：helmet、cors、express-rate-limit、compression
- 价格字段类型：MongoDB 中为 Number，已解决序列化问题
- 日志：winston
- 图片上传：multer，存到 /app/uploads 目录

## 当前代码文件结构

```
项目根目录
├── docker-compose.yml         # 三容器编排（MongoDB + Backend + Frontend）
├── .env                       # 环境变量（JWT_SECRET, MONGODB_URI等）
├── CLAUDE.md                  # 项目执行公约
├── Client/
│   ├── Dockerfile             # Nginx + React SPA 构建
│   ├── nginx.conf             # 前端 Nginx 配置
│   ├── src/                   # React 源码（46个.js文件）
│   └── build/                 # 构建产物
├── Server/
│   ├── Dockerfile             # Express 容器构建
│   ├── server.js              # 入口，含 helmet/CORS/rate-limit 等中间件
│   ├── config/                # 数据库、日志配置
│   ├── models/                # Mongoose 数据模型
│   ├── routes/                # 路由（用户、商品、购物车、上传）
│   ├── controllers/           # 控制器逻辑
│   ├── middleware/             # 中间件（认证、错误处理）
│   └── utils/                 # 工具函数
├── deploy/                    # 部署相关配置文件
└── 压力测试报告.md              # 包含性能基线数据
```

## 已实现的功能

- 用户注册/登录（JWT 认证）
- 商品发布、浏览、搜索（MongoDB 全文索引）
- 购物车管理
- 图片上传（multer）
- 用户个人中心
- 响应式布局（TailwindCSS）
- Docker 容器化部署
- Nginx 反代配置
- 安全加固（helmet / CORS / rate-limit / 数据脱敏）

## 已知问题 / 待改进

1. 测试阶段遗留的"测试大学"字符串替换代码（server.js 第 46-63 行），上线前需删除
2. 推荐系统已移除，后续可按需接入
3. 无 HTTPS 证书（当前仅 HTTP）
4. 无监控告警机制
5. 无数据库备份方案
6. 无 CI/CD 流水线
7. 无数据统计后台
8. 无内容审核机制

## 你的任务

请基于以上信息，生成一份包含以下 9 个模块的完整上线开发文档：

### 文档要求

1. **结构清晰**：每个模块有标题、描述、具体实施步骤、预期耗时
2. **分优先级**：P0（必须，不上线不能跑）、P1（强烈建议，上线前搞定）、P2（锦上添花，上线后可补）
3. **可执行**：每个任务给出具体的命令、配置示例或代码片段
4. **接地气**：考虑到 2 核 2G 服务器的资源限制，不要推荐过度设计的方案
5. **阶段性**：标注出哪些是「上线前必须做」、哪些是「上线后1-2周内补」、哪些是「稳定运营后逐步加」

### 9 个模块

**模块一：上线前安全检查**（P0）
- 检查并清理所有测试代码和测试数据
- 确保所有 API 接口有权限校验
- 检查 CORS 配置是否收紧到生产域名
- 确认 rate-limit 参数适合生产环境
- 检查密码强度、JWT 过期时间等安全策略

**模块二：HTTPS 配置**（P1）
- 推荐工具和方案（免费方案优先，如 certbot / acme.sh）
- Nginx 配置示例
- HTTP 强制跳转 HTTPS

**模块三：生产环境加固**（P1）
- 服务器安全（防火墙、SSH 配置、Fail2Ban 等）
- Docker 安全（最小权限、镜像扫描等）
- MongoDB 加固
- Nginx 安全头优化

**模块四：监控与告警**（P2→上线后）
- 推荐轻量级方案（资源占用要低）
- 核心监控指标（CPU/内存/QPS/错误率）
- 日志采集与轮转策略
- 宕机告警（邮件/短信/企业微信）

**模块五：数据备份与容灾**（P0）
- MongoDB 自动备份方案
- 备份存储策略（本地 + 远程）
- 恢复演练步骤
- 数据丢失止损方案

**模块六：CI/CD 流水线**（P2→上线后）
- 推荐轻量级方案（GitHub Actions 优先）
- 自动构建 + 自动部署示例
- 回滚策略

**模块七：运营后台与管理工具**（P1→上线后补）
- 必须的基础后台功能（用户管理、商品审核、数据统计）
- 推荐开发路径（MVP 版本即可）
- 考虑用现成的管理面板框架

**模块八：性能优化清单**（P1）
- 前端优化（图片懒加载、代码分割、CDN 等）
- 后端优化（数据库索引、查询优化、缓存策略）
- 2 核 2G 场景下的特殊调优建议

**模块九：应急预案**（P0）
- 常见故障场景及处理步骤
- 服务器宕机怎么办
- 数据库挂了怎么办
- 被攻击了怎么办
- 用户数据泄露应急流程

### 输出格式

请以 Markdown 格式输出，作为一个完整可复制的文档。
